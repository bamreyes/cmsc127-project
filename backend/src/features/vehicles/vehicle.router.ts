import { Router } from "express";
// import {} from "@/features/vehicle/vehicle.controller";

const router = Router();

// router.get("/", ...);
// router.post("/", ...);
// router.put("/", ...);
// router.delete("/", ...);

export default router;
