import { Router } from "express";
// import {} from "@/features/violation/violation.controller";

const router = Router();

// router.get("/", ...);
// router.post("/", ...);
// router.put("/", ...);
// router.delete("/", ...);

export default router;
